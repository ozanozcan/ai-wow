## Verification
- Commands run: `grep R-06 skills/mow/SKILL.md` hits automation hooks + triage pass; `grep R-07` hits automation hooks pass; `grep '# MOW'` intact pass
- Contract items: R-06 row orchestrator owner, no fourth lane → met; excludes lane tdd as round → met; R-07 list+stop → met
- Artifacts: `skills/mow/SKILL.md`
- Decisions honored: none pointed
